name: churn-env

channels:
  - defaults

dependencies:
  - python=3.12.7
  - pip

  - pip:
      - pandas
      - numpy
      - matplotlib
      - seaborn
      - scikit-learn
      - xgboost
      - mlflow==2.19.0
      - joblib